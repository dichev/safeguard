apiVersion: 1

providers:
- name: 'default'
  orgId: 1
  folder: ''
  type: file
  disableDeletion: false
  updateIntervalSeconds: 1 #how often Grafana will scan for changed dashboards  #TODO: during dev only is set to every 1 sec
  options:
    path: /var/lib/grafana/dashboards